<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🗂️ App-internal directories -->
    <files-path
        name="files"
        path="." />
    <cache-path
        name="cache"
        path="." />

    <!-- 💾 External app-specific directories -->
    <external-files-path
        name="external_files"
        path="." />
    <external-cache-path
        name="external_cache"
        path="." />

    <!-- 🧩 (Optional) Public Download location for updates -->
    <external-path
        name="public_downloads"
        path="Download/" />

    <external-path name="download" path="Download/" />
</paths>
