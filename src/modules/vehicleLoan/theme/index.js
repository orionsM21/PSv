import {createModuleTheme} from '../../../design-system/theme/moduleTheme';

export const vehicleTheme = createModuleTheme({
  id: 'vehicle',
  title: 'Vehicle Loan',
  accent: '#8BD3FF',
  gradient: ['#08111F', '#1B3A5B', '#C97872'],
});

export default vehicleTheme;
