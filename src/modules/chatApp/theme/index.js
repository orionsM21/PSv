import {createModuleTheme} from '../../../design-system/theme/moduleTheme';

export const chatTheme = createModuleTheme({
  id: 'chat',
  title: 'Chat App',
  accent: '#0F766E',
  gradient: ['#071C24', '#0F4C5C', '#1B8A8F'],
});

export default chatTheme;
