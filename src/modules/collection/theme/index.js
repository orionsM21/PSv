import {createModuleTheme} from '../../../design-system/theme/moduleTheme';

export const collectionTheme = createModuleTheme({
  id: 'collection',
  title: 'Collection',
  accent: '#0F766E',
  gradient: ['#071C24', '#0F4C5C', '#1B8A8F'],
});

export default collectionTheme;
